namespace led {
    /**
     *  make a graph with starting
     */
    //% block
    //% group="Advanced For Graph"
    export function graph(value: number, value2: number): void {

        led.plotBarGraph(value, value2)
    }
}
